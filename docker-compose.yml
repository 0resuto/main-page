services:
  website:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: main-page
    restart: unless-stopped
    ports:
      - "8080:3000"
    env_file:
      - ./frontend/.env